import { BookingsPageClient } from '@/components/booking/bookings-page';

export const metadata = { title: 'Mine Aftaler - CykelPlus' };

export default function BookingsPage() {
  return <BookingsPageClient />;
}
